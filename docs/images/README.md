# Capturas de tela

Esta pasta ainda não tem nenhuma imagem. Sugestões do que produzir e adicionar
aqui (com um nome de arquivo descritivo, ex. `grafico-top10-problemas.png`):

- Print do gráfico de barras da seção 4.4 do notebook (`Quantas linhas cada
  problema afeta`), gerado com Matplotlib.
- Print do painel final (Looker Studio / Data Studio) montado a partir de
  `painel_projetos.csv` e `painel_clientes.csv`, se e quando ele for
  publicado.
- Print da aba "Executar SQL" do DB Browser for SQLite rodando
  `sql/validacao.sql`, mostrando os vereditos "PASSOU".

Depois de adicionar as imagens, referencie-as no `README.md` principal.
